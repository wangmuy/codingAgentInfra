# AI 编程代理基础设施模板

一个用于将 [claude-code-infrastructure-showcase](https://github.com/diet103/claude-code-infrastructure-showcase) 适配到任何技术栈的模板，支持 AI 编程代理。

**用途：** 这个仓库是适配层 - 帮助你为特定项目需求创建展示库的自定义实例。最终输出是一个自包含的目录，你可以复制到你的项目中。

---

## 包含内容

此模板提供 [claude-code-infrastructure-showcase](https://github.com/diet103/claude-code-infrastructure-showcase) 的**适配层**。当你创建实例时，你将获得：

### 🤖 10 个专业代理（技术无关）
自主代理，适用于任何框架：
- `code-architecture-reviewer` - 审查代码架构
- `code-refactor-master` - 全面重构
- `documentation-architect` - 创建文档
- `frontend-error-fixer` - 修复前端错误
- `plan-reviewer` - 验证实现计划
- `refactor-planner` - 规划代码重组
- `web-research-specialist` - 研究技术问题
- `auth-route-tester` - 测试认证路由
- `auth-route-debugger` - 调试认证问题
- `auto-error-resolver` - 修复编译错误

### 🔌 2 个必要钩子
启用技能自动激活，适用于任何技术栈：
- `skill-activation-prompt.sh` - 根据上下文自动建议技能
- `post-tool-use-tracker.sh` - 跟踪文件更改

### 🛠️ 模板技能
你可以适配到自己技术栈的技能：

| 技能 | 用途 | 用例 |
|-------|---------|----------|
| **skill-developer** | 创建新技能 | ✅ 完全可移植 |
| **backend-dev-guidelines** | 后端模式 | 后端框架模板 |
| **frontend-dev-guidelines** | 前端模式 | 前端框架模板 |
| **route-tester** | 路由测试 | API/认证测试模板 |
| **error-tracking** | 监控模式 | 错误跟踪模板 |

### 📚 Dev 目录模式
跨会话维护项目上下文的方法论。

---

## 快速开始：创建你的自定义实例

使用你的 AI 编程代理为此模板创建一个针对你特定技术栈的自定义实例。

### 工作流程
```
用户："创建一个使用 React 作为前端、Express 作为后端的实例"

AI 代理：
1. 读取此模板
2. 创建 output/react-express-instance/ 并配置自定义设置
3. 为你的路径更新 skill-rules.json
4. 为你的设置创建 settings.json
5. 提供复制指令

你：
1. 将 output/react-express-instance/* 复制到你的项目
2. 运行：chmod +x .claude/hooks/*.sh
3. 完成 - 准备使用
```

### AI 代理创建的内容

AI 将在 `output/[你的项目名称]/` 中生成一个**自包含目录**，包含：

```
output/react-express-instance/
├── .claude/
│   ├── agents/            # 所有 10 个代理（可移植）
│   ├── hooks/             # 配置好的必要钩子
│   ├── skills/            # 模板技能 + 你的规则路径
│   └── settings.json      # 你的项目配置
├── dev/                   # Dev 文档方法论
└── AGENT_SETUP_VERIFY.md  # 快速验证步骤
```

### 复制粘贴命令
```bash
# AI 创建 output/react-express-instance/ 后
cp -r output/react-express-instance/* ~/your-project/
cd ~/your-project && chmod +x .claude/hooks/*.sh
```

---

## 模板维护者：内部工作流程

此部分供维护者增强此适配模板时使用。

### 当用户请求设置时

AI 遵循此工作流程：

1. **用户提示：** "使用 React + Express 创建实例"
2. **AI 操作：** 读取 `TEMPLATE_USAGE.md` 获取详细步骤
3. **AI 创建：** `output/react-express-instance/` 包含：
   - 所有代理已复制
   - 钩子已配置
   - 技能已选择（skill-developer + 模板技能）
   - `skill-rules.json` 包含项目特定路径
   - `settings.json` 已自定义
   - `AGENT_SETUP_VERIFY.md` 用于验证

4. **AI 指示：** 复制命令到用户项目

### 维护模板

**黄金法则：** 永远不要用技术特定代码修改模板文件。

**对此模板的补充应：**
- 技术无关
- 作为构建块工作
- 支持 `output/` 模式

**参见 `TEMPLATE_USAGE.md`：** 详细问卷、自定义示例和完整工作流程。

---

## 用法示例

### 使用代理（技术无关）

**示例：审查你的架构**
```
用户："使用 code-architecture-reviewer 代理检查我的控制器"

代理将：
- 分析所有控制器文件
- 检查模式一致性
- 验证是否符合最佳实践
- 返回带有修复的详细报告
```

**示例：计划重构**
```
用户："使用 refactor-planner 代理计划分解 Dashboard.tsx"

代理将：
- 分析组件
- 建议提取策略
- 创建分步计划
- 估算工作量
```

### 技能自动激活

技能基于**文件模式和关键词**激活，适用于任何技术栈：

```
用户："为用户个人资料创建新路由"

技能读取你的 skill-rules.json：
- 检查你是否正在编辑后端文件
- 匹配关键词 "route"
- 来自模板的相关模式

你编写代码 - 技能为你的框架
(Express, Fastify, NestJS 等) 提供指导
```

### 使用 Dev 文档

**对于跨多个会话的复杂任务：**

```bash
# 开始任务
/dev-docs implement multi-service architecture

# 创建 3 个文件到 dev/active/implement-multi-service-architecture/
# - plan.md: 战略概述
# - context.md: 当前状态和决策
# - tasks.md: 检查清单

# 上下文重置后：
# Claude 读取这些文件并立即恢复
```

---

## 自定义

### 路径模式

**这是最重要的自定义。** 更新 `.claude/skills/skill-rules.json` 以匹配你的项目结构：

```json
{
  "skills": {
    "your-skill-name": {
      "fileTriggers": {
        "pathPatterns": [
          "src/**/*.ts",           // ← 更新这些路径！
          "backend/**/*.ts",
          "services/**/*.ts"
        ]
      }
    }
  }
}
```

**常见模式：**
- `**/*.ts` - 所有 TypeScript 文件
- `api/**/*.ts` - 仅 API 路由
- `src/**/controller/*.ts` - 控制器模式
- `frontend/**/*.tsx` - React 组件

### 选择技能

**模板提供示例技能。** 根据需要选择：

- **skill-developer**：始终包含 - 让你创建新技能
- **其他技能**：作为模板复制，然后适配你的技术栈

示例流程：
```
用户有：Vue 3 + Fastify + MongoDB
→ 复制：skill-developer
→ 复制：backend-dev-guidelines（作为模板）
→ 创建：vue-fastify-skill（自定义）
```

### 配置 settings.json

```json
{
  "hooks": {
    "UserPromptSubmit": ["skill-activation-prompt"],
    "PostToolUse": ["post-tool-use-tracker"]
  },
  "permissions": {
    "allow": ["Skill(skill-developer)"],
    "deny": [],
    "ask": []
  },
  "project": {
    "name": "your-project-name",
    "type": "monorepo" | "single-app" | "microservices"
  }
}
```

---

## 目录结构

### 模板根目录（本仓库）
```
.claude/
├── agents/          # 10 个专业代理（技术无关）
├── hooks/           # 2 个必要钩子 + 配置
├── skills/          # 5 个示例技能 + skill-rules.json
└── README.md        # 每个技能的文档

dev/                 # Dev 文档方法论
└── README.md

README.md            # 此文件（主概述）
TEMPLATE_USAGE.md    # 为维护者创建 output/ 设置
output/              # 用户特定设置（gitignored）
```

### 安装后（用户项目）
```
your-project/
├── .claude/
│   ├── agents/
│   │   ├── code-architecture-reviewer.md
│   │   ├── code-refactor-master.md
│   │   └── ...（所有 10 个代理）
│   ├── hooks/
│   │   ├── skill-activation-prompt.sh    # 必须可执行！
│   │   ├── skill-activation-prompt.ts
│   │   ├── post-tool-use-tracker.sh      # 必须可执行！
│   │   ├── package.json
│   │   └── package-lock.json
│   ├── skills/
│   │   ├── skill-rules.json              # 必须更新！
│   │   ├── skill-developer/              # 创建新技能
│   │   └── ...（其他需要的技能）
│   └── settings.json                     # 钩子配置
└── dev/
    ├── README.md                         # Dev 文档指南
    └── active/                           # 你的任务文档
```

---

## 验证清单

设置后，验证：

- [ ] `.claude/hooks/*.sh` 文件可执行 (`chmod +x`)
- [ ] `skill-rules.json` 包含正确的项目路径模式
- [ ] `package.json` 和 `package-lock.json` 存在于 `.claude/hooks/`
- [ ] `settings.json` 格式正确
- [ ] 至少一个技能目录存在于 `.claude/skills/`

**快速命令：**
```bash
# 检查钩子是否可执行
ls -la .claude/hooks/*.sh

# 验证 JSON
cat .claude/settings.json | jq .

# 测试钩子
cd .claude/hooks && npx ts-node skill-activation-prompt.ts
```

---

## 故障排除

### 技能不激活？
1. 检查 `.claude/skills/skill-rules.json` 是否包含你的文件路径
2. 确认钩子可执行 (`chmod +x .claude/hooks/*.sh`)
3. 检查 `settings.json` 是否列出了钩子

### 代理不工作？
代理是独立的 `.md` 文件 - 应该立即工作。
用法：`"Use the [agent-name] agent to [task]"`

### 钩子错误？
```bash
cd .claude/hooks && npm install
chmod +x *.sh
```

---

## 常见工作流程

### 1. 代码审查
```
用户："使用 code-architecture-reviewer 检查我的控制器"
→ 返回分析和修复
```

### 2. 复杂任务
```
用户："/dev-docs implement feature X"
→ 创建 plan/context/tasks 文件

用户："使用计划构建"
→ 技能自动建议模式

用户："使用 plan-reviewer 验证"
→ 检查方法
```

### 3. 调试
```
用户："使用 frontend-error-fixer 处理构建错误”
→ 分析并建议修复
```

### 4. 重构
```
用户："/dev-docs refactor module"
用户："使用 refactor-planner 获取策略”
用户："使用 code-refactor-master 执行”
```

---

## 核心原则

1. **技术无关**: 适用于任何框架、语言或架构
2. **代理**: 独立的 `.md` 文件 - 复制即可使用
3. **技能**: 基于你的文件路径和关键词自动激活
4. **钩子**: 启用自动化 - 只需使它们可执行
5. **Dev 文档**: 在复杂的多会话工作中生存
6. **模板**: 保持纯净 - 自定义发生在用户项目的 `output/` 中

---

## 模板维护者

这是一个**模板仓库** - 永远不要在这里添加技术特定代码。

**当用户请求设置时：**
1. 阅读 `TEMPLATE_USAGE.md`
2. 创建 `output/[项目名称]/` 并使用自定义配置
3. 用户从 `output/` 复制到他们的项目
4. 模板保持通用和可重用

`output/` 方法让此模板保持通用，同时允许无限自定义。

---

## 更多信息

- **代理**: `.claude/agents/README.md` - 完整代理文档
- **技能**: `.claude/skills/README.md` - 技能设置和配置
- **钩子**: `.claude/hooks/README.md` - 钩子安装和故障排除
- **Dev 文档**: `dev/README.md` - 上下文管理方法论
- **工作流程**: `TEMPLATE_USAGE.md` - 完整维护者指南

---

**版本:** 1.0
**最后更新:** 2025-12-18
**技术理念:** 通用、可适配、技术无关
